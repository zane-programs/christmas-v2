import usePageTitle from "../../hooks/usePageTitle";

export default function Settings() {
  // set page title
  usePageTitle("Settings");

  return <div>Settings (still building out this page)</div>;
}
